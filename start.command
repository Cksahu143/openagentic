#!/usr/bin/env bash
# Double-click this file in Finder to start OpenAgent (opens a Terminal
# window and runs start.sh). If macOS blocks it the first time: right-click
# -> Open, then confirm.
cd "$(dirname "${BASH_SOURCE[0]}")"
./start.sh
